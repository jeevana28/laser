Run the following commands 
```bash
1. python3 -m venv .venv   (python version >= 3.7 and <= 3.11)
2. sourve .venv/bin/activate
3. cd coco
4. wget http://images.cocodataset.org/zips/val2014.zip
5. cd ..
6. pip install -r requirements.txt